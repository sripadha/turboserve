# NodePort 30080, which kind/kind-config.yaml maps to localhost:8080 on the host.
apiVersion: v1
kind: Service
metadata:
  name: turboserve-gateway
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
      nodePort: 30080
